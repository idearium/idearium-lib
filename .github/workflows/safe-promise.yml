name: '@idearium/safe-promise'
on:
    push:
        paths:
            - 'packages/safe-promise/**'
    release:
        types: [published]

jobs:
    test:
        name: Test
        runs-on: ubuntu-latest
        steps:
            - name: Setup
              uses: actions/checkout@v3
            - uses: actions/setup-node@v3
              with:
                cache: yarn
                node-version: lts/*
            - run: yarn
            - name: Test
              run: yarn workspace @idearium/safe-promise test
    publish:
        if: github.event_name == 'release' && contains(github.ref, '@idearium/safe-promise-v')
        name: Publish
        needs: test
        runs-on: ubuntu-latest
        steps:
            - name: Setup
              uses: actions/checkout@v3
            - uses: actions/setup-node@v3
              with:
                cache: yarn
                node-version: lts/*
            - run: yarn
            - name: Publish (beta)
              if: contains(github.ref, 'beta')
              run: yarn workspace @idearium/safe-promise publish --tag beta --access public
              env:
                  NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
            - name: Publish
              if: "!contains(github.ref , 'beta')"
              run: yarn workspace @idearium/safe-promise publish --access public
              env:
                  NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
