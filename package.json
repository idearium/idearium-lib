{
  "name": "idearium-lib",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky",
    "test": "npm test -ws --if-present"
  },
  "devDependencies": {
    "@idearium/eslint-config": "5.0.0",
    "husky": "9.1.7",
    "lint-staged": "17.0.7",
    "prettier": "3.8.4"
  }
}
